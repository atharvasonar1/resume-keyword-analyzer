# Data Folder

Place the job postings CSV file in this folder manually.

Use a filename such as `jobs.csv`.

Large datasets should not be committed to GitHub. The `.gitignore` file excludes CSV files in this folder.
